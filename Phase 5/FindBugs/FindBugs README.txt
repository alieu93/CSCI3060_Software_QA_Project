The image detailing the errors were all not fixed because it would be rudimentary and pointless.

For all dead stores to err for dead store to local variable, that is used for our error checking in case we get a DTF that has improper formatting, which means if we get proper DTFs, err will never be initialized.


The "write to static field from instance method" are all required for our methods for each transaction to work and attempting to fix them would require a total revamp of the code.


"Dead store to applyTest" is what we use for testing and is required.