Import this project into Eclipse with JUnit installed, since it was done in it.

Run the JUnit tests using "TestSuite.java", this will run both BackEndTest and MakeNewFilesTest and show the amount of statement and loop/decision coverage from both test classes.

The text files like daily_transaction_file.txt, available_tickets.txt and user_accounts.txt are also required in the main directory of the project.